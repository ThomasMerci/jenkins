apiVersion: v1
kind: Pod
metadata:
  name: www
  labels:
    app: www
spec:
  containers:
    - name: nginx
      image: nginx:1.14-alpine



apiVersion: v1
kind: Service
metadata:
  name: www
spec:
  selector: 
    app: www
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80




apiVersion: v1
kind: Pod
metadata:
  name: debug
spec:
  containers:
  - name: debug
    image: alpine
    command:
    - "sleep" 
    - "10000"











kubectl create -f www_pod.yaml

kubectl create -f www_service_clusterIP.yaml

kubectl create -f debug.yaml

kubectl exec -it debug -- sh
/ # apk update


/ # curl http://www:80
<!DOCTYPE html>
<html>



apiVersion: v1
kind: Service
metadata:
  name: www
spec:
  selector:
    app: www
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 31000

kubectl create -f www_service_clusterIP.yaml
docker network inspect bridge | findstr Gateway
                    "Gateway": "172.17.0.1"
curl http://172.17.0.1:31000
redirection:
kubectl port-forward --address 0.0.0.0 service/www 31000:80


docker run -d -p 8080:8080 -p 50000:50000 -v jenkins_home:/var/jenkins_home jenkins/jenkins:lts-jdk11

docker exec -it ebddec2151db bash
jenkins@ebddec2151db:/$ cat /var/jenkins_home/secrets/initialAdminPassword


curl https://mirrors.estointernet.in/apache/maven/maven-3/3.6.3/binaries/apache maven-3.6.3-bin.tar.gz --output apache-maven-3.6.3-bin.tar.gz
